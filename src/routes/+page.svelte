<h1>This is the root page, which does not load the large file</h1>
<h2>Manually navigate to /largefile to load the large file and cause the issue</h2>
<h2>Note that navigating using a CSR link (e.g. <a href="/largefile">/largefile</a>) does not seem to cause the issue</h2>